<!--Displays the show page for the selected position-->
<h2 align="center">Welcome, <%= @user.fname %></h2>
<div class="row">
	<div class="col-md-4 col-md-offset-4 center">
		<h4>Username: <%= @user.username %></h4>
		<%= link_to 'Edit Information', edit_user_path(@user), class: "btn btn-warning btn-md" %>
	</div>
</div>