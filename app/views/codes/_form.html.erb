<!--Contains the form partial-->
<!--Used for the new and edit actions-->
<%= render 'shared/errors', obj: @code %>


<div class="row">
	<div class="col-md-12">
		<%= form_for(@code, :html => {class: "form-horizontal", role: "form"}) do |f| %>
		<div class="form-group">
			<div class="col-sm-10">
				<%= f.text_field :code, class: "form-control", step: :any %>
			</div>
		</div>
		<div class = "form-group">
			<div class="col-md-offset-2 col-md-12">
				<% if logged_in? %>
					<%= f.submit "Edit code",class: "btn btn-primary btn-lg"  %>
				<% else %>
				<%= f.submit "Check code" ,class: "btn btn-primary btn-lg"  %>
				<% end %>
				<%= link_to "Return to home page", root_path, class: "btn btn-danger btn-lg"%> 
			</div>
		</div>
		<% end %>
	</div>
</div>
					